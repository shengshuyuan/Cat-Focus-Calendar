<p align="right"><strong>简体中文</strong> · <a href="skin-repair-log.md">English</a></p>

# 皮肤修复日志

## 2026-09-12 — 专注插画与倒计时边界

- 症状/复现：切换布偶或加菲后开启专注。两张专注 PNG 本身仍含睡猫；加菲专注图的脸部和右侧物品有错位片段。
- 证据/根因：`scratch/build_perfect_assets.py` 以休息帧为底，仅按颜色阈值复制专注帧 x=140..219、y=15..94 区域。猫主要位于该区域左侧，因此保留睡猫，同时复制了不对应的房间片段。`clock_app.c` 的状态映射已正确选择专注资源。
- 修复：基于休息参考编辑两张完整专注图，缩放为 230x107 后重新生成 RGB565。不要再对修复后的输入运行实验脚本。新增 `tools/export_pomodoro_assets.py --check` 校验六组 PNG/C、尺寸和行跨度。直接量化不透明颜色，也避免原转换器 255/256 透明度乘法带来的压暗。
- 其他已确认问题：专注或休息截止时按暂停，会留下不能继续也不能完成的零秒暂停态。修复为保持运行态，由正常 tick 发送完成事件；两阶段均补回归测试。
- 文档：旧文档的暂停姿势说明与实现对齐：专注暂停保持清醒图，待机/休息使用休息图。模型的放弃、跳过休息 API 没有 UI 入口；此次检查不新增按键操作。
- 验证：主机测试 PASS；六张 RGB565 资源校验 PASS；检查了固件数组渲染的 240x320 预览。这些是源码预览，不是 LVGL/真机截图。
- 实物验收：NOT RUN；未刷写设备。需真机检查开始、暂停、继续、切换皮肤及最后一秒暂停。生成的专注房间保留构图，但光照/细节可能与休息图略有差别，不声称背景逐像素一致。

- 构建结果：使用既有 Python 3.14 环境及 ESP-IDF v5.5.3，完整 `tools/validate.sh` 门禁 PASS。应用 1,945,872 / 3,145,728 字节；合并固件和受保护布局检查 PASS。已验证的合并产物为 `build/FoloToy-AI-Passport-full.bin`，未刷机。
